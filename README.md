#include<stdio.h>
int main()
{
	int a,b,sum,difference;
	printf("enter two numbers");
	scanf("%d %d",&a ,&b);
	sum=a+b;
	difference=a-b;
	printf("sum=%d\n",sum);
	printf("difference=%d\n",difference);
	return 0;
}
